---
sticker: emoji//1f7e2
---
Funded grants currently in progress. Each sub-folder `GRANT_YYYY_ACRONYM/` holds `_grant.md` (agency, amount, period, deliverables, linked projects), `proposal/` (approved version, immutable archive), `admin/` (contracts, financial reporting), `reporting/` (interim and final reports).

Feeds the KPI runway calculation. Don't create folders manually — move here from `submitted/` via `grant_transition.py --to active`.
