---
sticker: emoji//270d-fe0f
---
Grant proposals in active preparation. Each sub-folder `GRANT_YYYY_ACRONYM/` holds `_grant.md` (deadline, agency, amount requested), `drafts/`, and `notes/`.

Don't store operational reports or financial documents here — those belong in `active/[GRANT]/admin/` and `active/[GRANT]/reporting/` once the grant is funded.
