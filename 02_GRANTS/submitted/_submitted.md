---
sticker: emoji//1f6a9
---
Proposals submitted and awaiting a funding decision. Same internal structure as `writing/`, plus an `admin/` sub-folder for submission confirmation and agency correspondence.

Move here from `writing/` via `grant_transition.py --to submitted` — not manually, the script also updates `_grant.md`'s status and logs the transition.
