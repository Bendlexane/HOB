---
sticker: emoji//1f4c8
---

KPI database, monthly plans, annual reports. `kpi_collector.py` writes here nightly; the SQLite database itself and its exports are gitignored by design — they're your data, not template content.
