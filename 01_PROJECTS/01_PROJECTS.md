---
sticker: emoji//1f52c
---

Active research projects, one folder per project. High write velocity, low semantic metadata — this is where the day-to-day work happens. `archive_published.py` moves a project here to `99_ARCHIVE/` once its `status:` frontmatter reads `published`.
